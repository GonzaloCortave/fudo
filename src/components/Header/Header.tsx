import SearchBar from "../SearchBar/SearchBar";
import "./Header.css";

const Header = () => {
    return (
        <header className="Header">
            <SearchBar />
        </header>
    );
};

export default Header;
